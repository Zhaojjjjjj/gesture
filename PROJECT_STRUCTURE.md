# 项目结构说明

## 📁 目录结构

```
gesture/
├── src/                          # 源代码目录
│   ├── components/               # Vue 组件
│   │   └── HandTracking.vue      # 手势追踪主组件
│   ├── composables/              # 可复用的组合式函数
│   │   ├── useCamera.js          # 摄像头管理
│   │   ├── useHandTracking.js    # 手势识别逻辑
│   │   └── useARText.js          # AR 文本渲染逻辑
│   ├── utils/                    # 工具函数
│   │   └── canvas.js             # Canvas 绘制工具
│   ├── constants/                # 常量配置
│   │   └── config.js             # 全局配置常量
│   ├── App.vue                   # 根组件
│   └── main.js                   # 应用入口
├── index.html                    # HTML 入口文件
├── vite.config.js                # Vite 配置
├── package.json                  # 依赖管理
└── README.md                     # 项目说明

```

## 🏗️ 架构设计

### 1. **组件层 (Components)**
- `HandTracking.vue`: 主组件,负责协调各个 composable 和渲染逻辑

### 2. **逻辑层 (Composables)**
- `useCamera.js`: 封装摄像头初始化、启动、停止逻辑
- `useHandTracking.js`: 封装 MediaPipe 手势识别模型的加载和检测
- `useARText.js`: 封装 AR 文本生成和渲染逻辑

### 3. **工具层 (Utils)**
- `canvas.js`: 提供 Canvas 绘制的纯函数工具

### 4. **配置层 (Constants)**
- `config.js`: 集中管理所有配置常量,便于维护和修改

## 🎯 设计原则

1. **单一职责**: 每个模块只负责一个功能
2. **可复用性**: Composables 可以在其他组件中复用
3. **可测试性**: 纯函数和独立模块便于单元测试
4. **可维护性**: 清晰的目录结构和职责划分

## 🚀 启动项目

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build
```

## 📝 修改配置

所有配置项集中在 `src/constants/config.js` 中:

- **MediaPipe 配置**: 模型 URL、运行模式等
- **AR 配置**: 目标单词、字体样式、间距等
- **手部标记配置**: 标记颜色、大小等
- **摄像头配置**: 视频流参数

## 🔧 扩展功能

### 添加新的手势识别逻辑
1. 在 `useHandTracking.js` 中添加新的检测函数
2. 在 `HandTracking.vue` 中调用新函数

### 修改 AR 渲染效果
1. 在 `useARText.js` 中修改渲染逻辑
2. 在 `config.js` 中调整样式参数

### 添加新的工具函数
1. 在 `utils/` 目录下创建新文件
2. 导出纯函数供组件使用
